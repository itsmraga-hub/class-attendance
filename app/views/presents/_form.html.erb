<%= form_with(model: present) do |form| %>
  <% if present.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(present.errors.count, "error") %> prohibited this present from being saved:</h2>

      <ul>
        <% present.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :student_id, style: "display: block" %>
    <%= form.text_field :student_id %>
  </div>

  <div>
    <%= form.label :subject_id, style: "display: block" %>
    <%= form.text_field :subject_id %>
  </div>

  <div>
    <%= form.label :subject_date, style: "display: block" %>
    <%= form.date_field :subject_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
