<%= form_with(model: staff_course) do |form| %>
  <% if staff_course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(staff_course.errors.count, "error") %> prohibited this staff_course from being saved:</h2>

      <ul>
        <% staff_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :staff_id, style: "display: block" %>
    <%= form.text_field :staff_id %>
  </div>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
