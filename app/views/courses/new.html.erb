<% if current_admin %>
  <h1>New course</h1>
  <%= render "form", course: @course %>
  <br>
  <div>
    <%= link_to "Back to courses", courses_path %>
  </div>
<% else %>
  <p>Only admins can add courses</p>
<% end %>
